name: Dependency review
on:
  pull_request:
    branches: [main]
permissions: {}
jobs:
  dependency-review:
    permissions:
      contents: read # compares dependency changes in the pull request
      pull-requests: write # posts the dependency review summary
    uses: rknightion/.github/.github/workflows/dependency-review.yml@873962525bd50b84dfdb3226efab8c8e2cd6a9a2 # v1.11.0
