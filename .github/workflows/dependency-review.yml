name: Dependency review
on:
  pull_request:
    branches: [main]
permissions: {}
jobs:
  dependency-review:
    permissions:
      contents: read # compares dependency changes in the pull request
      pull-requests: write # posts the dependency review summary
    uses: rknightion/.github/.github/workflows/dependency-review.yml@5a2b8d059084a79d11c9675f2fd07abd7003f915 # v1.13.0
