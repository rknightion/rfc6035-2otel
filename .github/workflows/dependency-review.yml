name: Dependency review
on:
  pull_request:
    branches: [main]
permissions: {}
jobs:
  dependency-review:
    permissions:
      contents: read # compares dependency changes in the pull request
      pull-requests: write # posts the dependency review summary
    uses: rknightion/.github/.github/workflows/dependency-review.yml@7be903bd193f76b1a590cbe54943a9bf9592ce45 # v1.18.0
